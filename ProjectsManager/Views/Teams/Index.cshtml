@model IEnumerable<ProjectsManager.Models.Team>

@{
    ViewData["Title"] = "Список команд";
}

<h1 class="form-title">Список команд</h1>

<p>
    <a class="form-link" asp-action="Create">Создать новую команду</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreationDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreationDate)
            </td>
            <td>
                    <a class="form-link" asp-action="Edit" asp-route-id="@item.Id">Редактировать</a> |
                    <a class="form-link" asp-action="Details" asp-route-id="@item.Id">Детали</a> |
                    <a class="form-link" asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
