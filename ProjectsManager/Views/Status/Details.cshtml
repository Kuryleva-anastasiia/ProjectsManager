@model ProjectsManager.Models.Status

@{
    ViewData["Title"] = "Детали статуса";
}

<h1 class="form-title">Детали статуса</h1>

<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a class="form-link" asp-action="Edit" asp-route-id="@Model?.Id">Редактировать</a> 
</div>
